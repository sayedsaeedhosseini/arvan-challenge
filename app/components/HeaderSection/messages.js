/*
 * HeaderTitle Messages
 *
 * This contains all the text for the HeaderTitle component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderTitle';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderTitle component!',
  },
});
