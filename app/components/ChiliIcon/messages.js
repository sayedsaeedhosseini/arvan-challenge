/*
 * ChiliIcon Messages
 *
 * This contains all the text for the ChiliIcon component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ChiliIcon';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChiliIcon component!',
  },
});
