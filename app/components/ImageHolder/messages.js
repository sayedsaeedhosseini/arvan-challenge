/*
 * ImageHolder Messages
 *
 * This contains all the text for the ImageHolder component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ImageHolder';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ImageHolder component!',
  },
});
