/*
 * TabsMenu Messages
 *
 * This contains all the text for the TabsMenu component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TabsMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TabsMenu component!',
  },
});
