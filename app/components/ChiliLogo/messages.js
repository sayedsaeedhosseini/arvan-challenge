/*
 * ChiliLogo Messages
 *
 * This contains all the text for the ChiliLogo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ChiliLogo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChiliLogo component!',
  },
});
