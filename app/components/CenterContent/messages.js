/*
 * CenterContent Messages
 *
 * This contains all the text for the CenterContent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CenterContent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CenterContent component!',
  },
});
